/**
 * Shared constants used across multiple dashboard/app pages.
 * Centralised here so a single update propagates everywhere.
 */

// Desktop app download URLs (branded redirects via Netlify → GitHub Releases)
export const MACOS_URL = '/download/macos'
export const WINDOWS_URL = '/download/windows'
